#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * Return: Absolute value of integer
 * @x: Integer variable
 */
int _abs(int x)
{
	if (x < 0)
	{
		return (-x);
	}
	else
	{
		return (x);
	}
}
